# Salita Quest 5.5.6 — Canonical Avatar Rebuild

This release replaces the mixed PNG, WebP, SVG-wrapper, sprite-crop, and canvas-rewrite artwork pipeline with one canonical 48-avatar PNG set supplied by the project owner.

The runtime now uses stable avatar IDs, one catalogue, direct image assignment, bounded fallback handling, and no document-wide image observers.
