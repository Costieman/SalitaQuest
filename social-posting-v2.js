(() => {
  "use strict";

  const INSTALL_FLAG = "__salitaQuestSocialPostingV2Installed";
  const PROFILE_STORE = "salitaQuestLocalProfilesV1";
  const ACTIVE_PROFILE = "salitaQuestActiveProfileId";
  const MAX_CHEST = 6;
  let currentShare = null;

  const PLATFORM_META = {
    facebook:{label:"Facebook",icon:"f",publicLabel:"Open share composer"},
    instagram:{label:"Instagram",icon:"◎",publicLabel:"Share image via device"},
    tiktok:{label:"TikTok",icon:"♪",publicLabel:"Share image via device"},
    x:{label:"X",icon:"𝕏",publicLabel:"Open post composer"},
    linkedin:{label:"LinkedIn",icon:"in",publicLabel:"Open share composer"},
    whatsapp:{label:"WhatsApp",icon:"◉",publicLabel:"Send invitation"}
  };

  function retry(){ window.setTimeout(install,100); }
  function readStore(){ try{return JSON.parse(localStorage.getItem(PROFILE_STORE)||"null")||{profiles:[]};}catch{return {profiles:[]};} }
  function activeProfile(){ const store=readStore();const id=sessionStorage.getItem(ACTIVE_PROFILE);return store.profiles?.find(profile=>profile.id===id)||null; }
  function avatarPath(){ const valid=new Set(["tarsier","eagle","tamaraw","peacock","orchid","jade","rafflesia","anahaw"]);const id=activeProfile()?.avatarId;return `avatars/${valid.has(id)?id:"tarsier"}.png`; }
  function courseLabel(){ return document.body.dataset.course==="cebuano"?"Cebuano / Bisaya":"Tagalog"; }
  function shareRoot(campaign){ try{const url=new URL("./",location.href);url.hash="";url.search="";if(/^https?:$/.test(url.protocol)){url.searchParams.set("ref",campaign);return url.toString();}}catch{}return `https://costieman.github.io/SalitaQuest/?ref=${encodeURIComponent(campaign)}`; }
  function badgeById(id){ try{return BADGES.find(badge=>badge.id===id)||null;}catch{return null;} }
  function isEarned(badge){ try{return Boolean(badge?.test?.(state));}catch{return false;} }
  function chestBadges(){ const ids=state.badgeProgress?.chestIds||[];return ids.slice(0,MAX_CHEST).map(badgeById).filter(badge=>badge&&isEarned(badge)); }
  function notify(message){ try{if(typeof toast==="function"){toast(message);return;}}catch{}console.info(message); }
  function escapeHTML(value){return String(value??"").replace(/[&<>"']/g,ch=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[ch]));}

  function loadImage(src){ return new Promise(resolve=>{const image=new Image();image.onload=()=>resolve(image);image.onerror=()=>resolve(null);image.src=src;}); }
  function roundRect(ctx,x,y,w,h,r){const radius=Math.min(r,w/2,h/2);ctx.beginPath();ctx.moveTo(x+radius,y);ctx.arcTo(x+w,y,x+w,y+h,radius);ctx.arcTo(x+w,y+h,x,y+h,radius);ctx.arcTo(x,y+h,x,y,radius);ctx.arcTo(x,y,x+w,y,radius);ctx.closePath();}
  function wrapText(ctx,text,x,y,maxWidth,lineHeight,maxLines=3){const words=String(text||"").split(/\s+/).filter(Boolean);const lines=[];let line="";for(const word of words){const test=line?`${line} ${word}`:word;if(line&&ctx.measureText(test).width>maxWidth){lines.push(line);line=word;}else line=test;}if(line)lines.push(line);lines.slice(0,maxLines).forEach((value,index)=>ctx.fillText(value,x,y+index*lineHeight));}
  function canvas(){const node=document.createElement("canvas");node.width=1080;node.height=1080;return node;}
  function drawBackground(ctx){const gradient=ctx.createLinearGradient(0,0,1080,1080);gradient.addColorStop(0,"#08152d");gradient.addColorStop(.52,"#123e49");gradient.addColorStop(1,"#087166");ctx.fillStyle=gradient;ctx.fillRect(0,0,1080,1080);ctx.fillStyle="rgba(247,201,72,.13)";ctx.beginPath();ctx.arc(930,95,260,0,Math.PI*2);ctx.fill();ctx.beginPath();ctx.arc(100,1010,290,0,Math.PI*2);ctx.fill();}
  function drawBrand(ctx,title){const profile=activeProfile();ctx.textAlign="left";ctx.fillStyle="#f7c948";ctx.font="900 30px system-ui,sans-serif";ctx.fillText("SALITA QUEST",64,72);ctx.fillStyle="#fff";ctx.font="900 53px system-ui,sans-serif";ctx.fillText(title,64,132);ctx.fillStyle="rgba(255,255,255,.74)";ctx.font="700 23px system-ui,sans-serif";ctx.fillText(`${profile?.name||"A learner"} · ${courseLabel()}`,66,172);}
  function drawFooter(ctx,campaign){const url=shareRoot(campaign);const display=url.replace(/^https?:\/\//,"").replace(/\?.*$/,"").replace(/\/$/,"");ctx.textAlign="center";ctx.fillStyle="#f7c948";ctx.font="900 29px system-ui,sans-serif";ctx.fillText("Learn Filipino languages for free",540,988);ctx.fillStyle="#fff";ctx.font="800 25px system-ui,sans-serif";ctx.fillText("with Salita Quest",540,1025);ctx.fillStyle="rgba(255,255,255,.72)";ctx.font="700 18px system-ui,sans-serif";ctx.fillText(display,540,1054);}

  async function drawAvatar(ctx,x,y,size){const avatar=await loadImage(avatarPath());ctx.save();roundRect(ctx,x,y,size,size,size*.23);ctx.fillStyle="#e9f6f1";ctx.fill();ctx.strokeStyle="#f7c948";ctx.lineWidth=Math.max(6,size*.025);ctx.stroke();ctx.clip();if(avatar){ctx.imageSmoothingEnabled=false;ctx.drawImage(avatar,x,y,size,size);}ctx.restore();return avatar;}
  async function drawBadgeVisual(ctx,badge,x,y,size,avatar){const custom=await loadImage(badge?.image||"");ctx.save();roundRect(ctx,x,y,size,size,size*.23);ctx.fillStyle="#f8f2d8";ctx.fill();ctx.strokeStyle="#f7c948";ctx.lineWidth=Math.max(5,size*.03);ctx.stroke();ctx.clip();if(custom){ctx.drawImage(custom,x,y,size,size);}else if(avatar){ctx.globalAlpha=.92;ctx.imageSmoothingEnabled=false;ctx.drawImage(avatar,x,y,size,size);ctx.globalAlpha=1;ctx.fillStyle="rgba(6,20,42,.72)";ctx.beginPath();ctx.arc(x+size*.76,y+size*.76,size*.19,0,Math.PI*2);ctx.fill();ctx.fillStyle="#fff";ctx.font=`900 ${Math.round(size*.20)}px system-ui,sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(badge?.icon||"★",x+size*.76,y+size*.76);}else{ctx.fillStyle="#12213d";ctx.font=`900 ${Math.round(size*.38)}px system-ui,sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(badge?.icon||"★",x+size/2,y+size/2);}ctx.restore();}

  async function buildBadgeCard(badge){const node=canvas(),ctx=node.getContext("2d");drawBackground(ctx);drawBrand(ctx,"BADGE EARNED");const avatar=await loadImage(avatarPath());await drawBadgeVisual(ctx,badge,330,220,420,avatar);ctx.textAlign="center";ctx.textBaseline="alphabetic";ctx.fillStyle="#f7c948";ctx.font="900 26px system-ui,sans-serif";ctx.fillText(String(badge.category||"Achievement").toUpperCase(),540,690);ctx.fillStyle="#fff";ctx.font="900 54px system-ui,sans-serif";wrapText(ctx,badge.name,540,760,860,60,2);ctx.fillStyle="rgba(255,255,255,.78)";ctx.font="700 25px system-ui,sans-serif";wrapText(ctx,badge.description,540,870,820,34,2);drawFooter(ctx,"badge-share");return node;}
  async function buildChestCard(badges){const node=canvas(),ctx=node.getContext("2d");drawBackground(ctx);drawBrand(ctx,"MY BADGE CHEST");const avatar=await loadImage(avatarPath());if(avatar){ctx.save();roundRect(ctx,845,42,150,150,36);ctx.fillStyle="#e9f6f1";ctx.fill();ctx.clip();ctx.imageSmoothingEnabled=false;ctx.drawImage(avatar,845,42,150,150);ctx.restore();}const positions=[[62,238],[386,238],[710,238],[62,570],[386,570],[710,570]];for(let index=0;index<MAX_CHEST;index+=1){const [x,y]=positions[index],badge=badges[index];ctx.save();roundRect(ctx,x,y,290,294,27);ctx.fillStyle="rgba(7,18,37,.72)";ctx.fill();ctx.strokeStyle=badge?"rgba(247,201,72,.52)":"rgba(255,255,255,.14)";ctx.lineWidth=3;ctx.stroke();ctx.restore();if(badge){await drawBadgeVisual(ctx,badge,x+65,y+20,160,avatar);ctx.textAlign="center";ctx.fillStyle="#fff";ctx.font="900 23px system-ui,sans-serif";wrapText(ctx,badge.name,x+145,y+220,250,27,2);}else{ctx.textAlign="center";ctx.fillStyle="rgba(255,255,255,.25)";ctx.font="900 58px system-ui,sans-serif";ctx.fillText("＋",x+145,y+145);}}drawFooter(ctx,"badge-chest");return node;}
  function canvasBlob(node){return new Promise((resolve,reject)=>node.toBlob(blob=>blob?resolve(blob):reject(new Error("Could not create social card")),"image/png",1));}
  function blobToDataUrl(blob){return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(reader.result);reader.onerror=reject;reader.readAsDataURL(blob);});}

  function ensureModal(){let modal=document.getElementById("socialPostModal");if(modal)return modal;modal=document.createElement("div");modal.id="socialPostModal";modal.className="social-post-modal";modal.hidden=true;modal.innerHTML=`<div class="social-post-backdrop" data-social-close></div><section class="social-post-card" role="dialog" aria-modal="true" aria-labelledby="socialPostTitle"><button class="social-post-close" type="button" data-social-close aria-label="Close">×</button><div class="social-post-preview"><img id="socialPostPreview" alt="Generated Salita Quest social card"><small>Your pixel avatar is used whenever a custom badge illustration is not available.</small></div><div class="social-post-content"><p class="eyebrow">SHARE YOUR PROGRESS</p><h2 id="socialPostTitle">Post achievement</h2><p id="socialPostDescription"></p><div id="socialPlatformGrid" class="social-platform-grid"></div><div class="social-post-secondary"><button type="button" data-social-native>Share image to an app</button><button type="button" data-social-download>Download card</button><button type="button" data-social-copy>Copy caption</button></div><p id="socialPostStatus" class="social-post-status"></p><p class="social-post-connection-note">Connected-account image posting needs a secure OAuth service and provider approval. <button type="button" data-social-manage>Manage connected accounts</button></p></div></section>`;document.body.appendChild(modal);return modal;}
  function setStatus(message,error=false){const node=document.getElementById("socialPostStatus");if(node){node.textContent=message||"";node.style.color=error?"#ff8c8c":"#f7c948";}}
  function connectionApi(){return window.SalitaQuestSocialConnections||null;}
  function platformButton(provider){const meta=PLATFORM_META[provider],connected=Boolean(connectionApi()?.isConnected?.(provider));let detail=connected?"Direct image post":"";if(!detail){if(["instagram","tiktok"].includes(provider))detail=navigator.share?"Send the image to the installed app":"Connect an approved account to post";else detail=meta.publicLabel;}return `<button class="social-platform-action ${connected?"connected":""}" type="button" data-social-platform="${provider}"><span class="social-platform-icon">${meta.icon}</span><span><strong>${meta.label}</strong><small>${detail}</small></span></button>`;}
  function renderModal(){const grid=document.getElementById("socialPlatformGrid");if(grid)grid.innerHTML=Object.keys(PLATFORM_META).map(platformButton).join("");}

  async function openShare(type,id){const badge=type==="badge"?badgeById(id):null;const badges=type==="chest"?chestBadges():[];if(type==="badge"&&(!badge||!isEarned(badge)))return;if(type==="chest"&&!badges.length)return;const modal=ensureModal();modal.hidden=false;document.body.classList.add("social-post-busy");setStatus("Preparing a high-quality social card…");try{const node=type==="badge"?await buildBadgeCard(badge):await buildChestCard(badges);const blob=await canvasBlob(node);const url=shareRoot(type==="badge"?"badge-share":"badge-chest");const title=type==="badge"?`My ${badge.name} badge`:"My Salita Quest Badge Chest";const text=type==="badge"?`I earned the ${badge.name} badge while learning ${courseLabel()} with Salita Quest.`:`These are my proudest Salita Quest achievements while learning ${courseLabel()}.`;const caption=`${text} Learn Filipino languages for free with Salita Quest: ${url}`;const fileName=type==="badge"?`salita-quest-${badge.id}.png`:"salita-quest-badge-chest.png";currentShare={type,badge,badges,node,blob,title,text,caption,url,fileName};document.getElementById("socialPostPreview").src=URL.createObjectURL(blob);document.getElementById("socialPostTitle").textContent=title;document.getElementById("socialPostDescription").textContent=text;renderModal();setStatus("Choose where to post it.");}catch(error){console.error(error);setStatus(error.message||"The social card could not be created.",true);}finally{document.body.classList.remove("social-post-busy");}}
  function closeModal(){const modal=document.getElementById("socialPostModal");if(modal)modal.hidden=true;const preview=document.getElementById("socialPostPreview");if(preview?.src?.startsWith("blob:")){URL.revokeObjectURL(preview.src);preview.removeAttribute("src");}currentShare=null;}
  function popup(url){window.open(url,"salitaSocialPost","popup=yes,width=720,height=760");}
  function publicComposer(provider){const share=currentShare;if(!share)return;const encodedUrl=encodeURIComponent(share.url),encodedText=encodeURIComponent(share.caption);if(provider==="facebook")popup(`https://www.facebook.com/sharer/sharer.php?u=${encodedUrl}`);else if(provider==="x")popup(`https://twitter.com/intent/tweet?text=${encodedText}`);else if(provider==="linkedin")popup(`https://www.linkedin.com/sharing/share-offsite/?url=${encodedUrl}`);else if(provider==="whatsapp")popup(`https://wa.me/?text=${encodedText}`);}
  async function nativeShare(){const share=currentShare;if(!share)return;const file=new File([share.blob],share.fileName,{type:"image/png"});if(!navigator.share)throw new Error("This browser does not provide app sharing.");const payload={title:share.title,text:share.text,url:share.url};if(navigator.canShare?.({files:[file]}))payload.files=[file];await navigator.share(payload);}
  async function connectedPost(provider,button){const share=currentShare,api=connectionApi();if(!share||!api?.isConnected?.(provider))return false;button.disabled=true;setStatus(`Posting to ${PLATFORM_META[provider].label}…`);try{const imageDataUrl=await blobToDataUrl(share.blob);const result=await api.post(provider,{title:share.title,caption:share.caption,url:share.url,imageDataUrl,imageMime:"image/png"});setStatus(result.message||`Posted to ${PLATFORM_META[provider].label}.`);return true;}catch(error){setStatus(error.message||"The connected post failed.",true);return true;}finally{button.disabled=false;}}
  async function platformAction(provider,button){if(await connectedPost(provider,button))return;if(["facebook","x","linkedin","whatsapp"].includes(provider)){try{await navigator.clipboard?.writeText(currentShare.caption);}catch{}publicComposer(provider);setStatus(`${PLATFORM_META[provider].label} opened. The caption was copied when supported.`);return;}if(["instagram","tiktok"].includes(provider)){try{await nativeShare();}catch(error){setStatus(`${PLATFORM_META[provider].label} direct image posting requires a connected approved account or mobile app sharing.`,true);}return;}}
  function download(){if(!currentShare)return;const link=document.createElement("a");link.href=URL.createObjectURL(currentShare.blob);link.download=currentShare.fileName;document.body.appendChild(link);link.click();link.remove();setTimeout(()=>URL.revokeObjectURL(link.href),1500);setStatus("Social card downloaded.");}
  async function copyCaption(){if(!currentShare)return;try{await navigator.clipboard.writeText(currentShare.caption);setStatus("Caption and referral link copied.");}catch{setStatus("The browser could not copy the caption.",true);}}

  function install(){if(window[INSTALL_FLAG])return;try{if(typeof BADGES==="undefined"||typeof state==="undefined"){retry();return;}}catch{retry();return;}window[INSTALL_FLAG]=true;ensureModal();document.addEventListener("click",event=>{const badge=event.target.closest("[data-share-badge]");const chest=event.target.closest("[data-share-badge-chest]");if(badge||chest){event.preventDefault();event.stopImmediatePropagation();openShare(badge?"badge":"chest",badge?.dataset.shareBadge||"");return;}if(event.target.closest("[data-social-close]")){closeModal();return;}const platform=event.target.closest("[data-social-platform]");if(platform){platformAction(platform.dataset.socialPlatform,platform);return;}if(event.target.closest("[data-social-native]")){nativeShare().catch(error=>setStatus(error.message,true));return;}if(event.target.closest("[data-social-download]")){download();return;}if(event.target.closest("[data-social-copy]")){copyCaption();return;}if(event.target.closest("[data-social-manage]")){closeModal();connectionApi()?.openSettings?.();}},true);document.addEventListener("keydown",event=>{if(event.key==="Escape"&&!document.getElementById("socialPostModal")?.hidden)closeModal();});window.SalitaQuestSocialPosting={openBadge:id=>openShare("badge",id),openChest:()=>openShare("chest","")};}
  install();
})();
